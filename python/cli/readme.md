# Cuboid Volume Calculator (Python CLI)

A simple Python command-line tool that calculates the **volume of a cuboid** using user-supplied dimensions.

This project is designed as an **introduction to `argparse`**, command-line arguments, and basic Python program structure.

---

## 📐 What the Program Does

The program calculates the volume of a cuboid using the formula:


All values are provided via the command line.

---

## 🧠 Concepts Covered

This script introduces:

- Functions
- Command-line interfaces (CLI)
- The `argparse` module
- Positional arguments
- `__name__ == "__main__"` explained
- Basic mathematical modelling in Python

---

## ▶️ How to Run the Program

From the directory containing the file:

```bash
python3 cuboid_volume.py <length> <width> <height>
